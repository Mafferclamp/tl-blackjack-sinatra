<div class="col-md-6">
	<h3>Black Jack</h3>
<!-- 	<p>You bet $<%= session[:bet]%></p>
	<p>Dealer's cards are: </p>
		<ul>
			<% session[:dealer_cards].each do |card| %>
			<li><%= card.to_s %></li>
			<% end %>
		</ul>
	<p>Player's cards are:</p>  
		<ul>
			<% session[:player_cards].each do |card| %>
			<li><%= card.to_s %></li>
			<% end %>
		</ul> -->

</div>